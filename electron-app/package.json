{
  "name": "e-pluribus",
  "productName": "E Pluribus",
  "version": "1.0.0",
  "description": "E Pluribus",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "electron .",
    "build-html": "npm run --prefix .. build-prod && cp -r ../public .",
    "build-app": "electron-packager . 'E Pluribus' --overwrite --platform=all --arch=x64 --icon=icons/logo --prune=true --out=release-builds"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.7.10",
    "electron-packager": "^10.1.0"
  },
  "dependencies": {}
}
